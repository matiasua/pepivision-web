Numeración reestructurada tras la corrección de taxonomía (dos categorías definitivas: Lentes ópticos, Lentes de sol — ver design.md → "Taxonomía definitiva de categorías" y "Fase 5 — evaluación contra la nueva taxonomía"). La numeración original (Fases 1–10) no se conserva donde generaba una secuencia incorrecta, pero cada tarea previamente completada permanece trazable — ver la "Tabla de trazabilidad" al final de este documento. Ningún checkbox marcado en las Fases 1–4 se oculta; las partes de la Fase 5 que ya no satisfacen la nueva taxonomía quedan explícitamente reabiertas, no ocultas.

## 0. Precondición de secuencia (bloqueante, no iniciar el resto sin esto)

- [x] 0.1 Confirmar que `add-pepi-vision-360-v1` está completado y archivado (`openspec/specs/` contiene sus capacidades). No iniciar la Fase 1 antes de esto — ver design.md → "Secuencia con `add-pepi-vision-360-v1`".

## 1. Modelo de datos — ya completado, sin cambios

- [x] 1.1 Agregar `Category`, `ProductOffering`, `CategoryAttributeDefinition`, `ProductOfferingAttributeValue` y el enum `CategoryAttributeType` a `prisma/schema.prisma`.
- [x] 1.2 Generar y aplicar la migración Prisma dentro de Docker Compose.
- [x] 1.3 Verificar que `Product`/`ProductColor`/`ProductImage`/`Brand`/`Request`/`RequestAttachment` quedan intactos.
- [x] 1.4 Implementar `categoryCapabilitiesSchema` (Zod), sin cambios de forma tras la corrección de taxonomía.
- [x] 1.5 Implementar `offeringConfigurationSchema` (Zod, versionado).

## 2. Categorías y seed — ya completados parcialmente, valores de seed reabiertos en Fase 5

- [x] 2.1 Implementar repositorio/servicio de `Category` (CRUD, validación de capabilities, slugify + unicidad). Sigue válido — agnóstico al número de categorías.
- [ ] 2.2 ~~Extender `prisma/seed.ts` con un seed idempotente de las tres categorías iniciales~~ **REABIERTA**: el seed debe declarar exactamente las dos categorías definitivas (Lentes ópticos, Lentes de sol) desde ahora en adelante. Ver 5.2 para el remapeo de las filas ya sembradas con tres categorías en instalaciones existentes.
- [x] 2.3 Prueba: correr el seed dos veces no duplica categorías. Sigue válida (comportamiento de idempotencia, agnóstico al conteo).
- [x] 2.4 Prueba: capabilities malformadas se rechazan al escribir / fail-closed al leer. Sigue válida, sin cambios.

## 3. ProductOfferings — ya completado, sin cambios

- [x] 3.1 Repositorio/servicio de `ProductOffering` (crear/actualizar/soft-delete), `@@unique([productId, categoryId])`, `@@unique([categoryId, slug])`.
- [x] 3.2 Helper de verificación de pertenencia oferta↔categoría↔producto.
- [x] 3.3 `ProductOffering.priceFromClp` como única fuente de precio dentro del dominio `ProductOffering` — ver design.md → "Fase de compatibilidad de precios".
- [x] 3.4 Prueba: un mismo `Product` en dos categorías no duplica `ProductColor` ni `ProductImage`.
- [x] 3.5 Prueba: precios distintos por categoría para el mismo `Product`.
- [x] 3.6 Prueba: crear una segunda oferta para el mismo `(productId, categoryId)` es rechazado.
- [x] 3.7 Prueba: una oferta que no pertenece a la categoría reclamada es rechazada.
- [x] 3.8 Prueba: categoría activa/inactiva y oferta visible/invisible se incluyen/excluyen correctamente de las consultas públicas.
- [x] 3.9 Prueba: editar `Product.priceFromClp` después de creada una oferta no altera el precio ya publicado de esa oferta.

## 4. Administración — ya completada; extendida en Fase 6 (imágenes de categoría)

- [x] 4.1 `/admin/categories` (listar/crear/editar/reordenar/activar-desactivar/slug/SEO/capabilities/atributos), SUPERADMIN-only.
- [x] 4.2 Sección "Disponibilidad en el catálogo" en `ProductForm.tsx` (ADMIN y SUPERADMIN).
- [x] 4.3 Eliminación bloqueada con ofertas asociadas.
- [x] 4.4 Auditoría de categorías/ofertas.
- [x] 4.5 Prueba: un `ADMIN` no puede alterar la estructura de categorías pero sí puede administrar ofertas.
- [x] 4.6 Prueba: eliminar una categoría con ofertas asociadas queda bloqueado; desactivarla funciona.
- [ ] 4.7 **NUEVA** — extender `CategoryForm.tsx` con el flujo de imagen de categoría (ver Fase 6): esta tarea vive aquí porque es parte de la administración de categorías, ejecutada después de que la Fase 6 defina el pipeline de subida.

## 5. Corrección de taxonomía a dos categorías (bloqueante para 6–16)

- [x] 5.1 Renombrar in-place la categoría `lentes-de-sol-opticos` → slug `lentes-de-sol`, nombre "Lentes de sol" (mismo `id`, cero ofertas afectadas) — ver design.md → "Migración de datos: dos categorías", paso 1. Implementado en `modules/catalog/taxonomy-migration.ts#migrateToDefinitiveTaxonomy()`; verificado contra la base de desarrollo real (`prisma db seed`): el `id` de la categoría no cambió.
- [x] 5.2 Remapear cada `ProductOffering` de la categoría `armazones` hacia `lentes-opticos`: reasignar `categoryId` cuando el producto no tenga ya una oferta en `lentes-opticos`; listar para revisión manual admin-por-admin los productos que ya tengan ambas (ver design.md, paso 2 — sin criterio de desempate automático, requiere decisión humana). Implementado en la misma función; el caso de conflicto se reporta en `TaxonomyMigrationReport.conflicts` y nunca se fusiona/sobrescribe automáticamente (cubierto en `tests-integration/category-taxonomy-migration.test.ts`).
- [x] 5.3 Eliminar la fila `Category` de `armazones` una vez confirmado que no le queda ninguna `ProductOffering` (reutilizando el mecanismo ya implementado de bloqueo de borrado con ofertas asociadas como verificación). Verificado en la base real: la categoría `armazones` (0 ofertas) fue eliminada tras `prisma db seed`.
- [x] 5.4 Actualizar `prisma/seed.ts` (tarea 2.2 reabierta): `SEED_CATEGORIES` declara exactamente Lentes ópticos y Lentes de sol; ninguna instalación nueva vuelve a sembrar `armazones`.
- [x] 5.5 Actualizar la tabla de capabilities de las dos categorías definitivas en el seed (`requiresColor/allowsLensType/allowsTreatments/allowsPrescription/allowsPrescriptionAttachment/allowsLensTint/allowsFrameSelection`) según design.md → "Valores de las dos categorías definitivas".
- [x] 5.6 Prueba: el remapeo es idempotente (correrlo dos veces no duplica ni vuelve a mover ofertas ya remapeadas). `tests/taxonomy-migration.test.ts` (unit, mocked) + `tests-integration/category-taxonomy-migration.test.ts` (Postgres real) + `tests-integration/category-seed.test.ts` (seed corrido dos veces).
- [x] 5.7 Prueba: ninguna `ProductOffering`/`Request` histórica pierde datos durante el remapeo (mismo estándar que 3.4–3.9 y las pruebas de migración de la Fase 15). Verificado en integración: slug/precio/`id` de la oferta remapeada permanecen idénticos; filas en conflicto tampoco se alteran.

## 6. Imágenes de categorías (nueva, ver design.md → "Imágenes de categoría")

- [x] 6.1 Implementar `processCategoryImage` (o extender `processProductImage` con salida configurable) — validación MIME real vía `sharp`, salida JPEG y opción WebP. Implementado extrayendo un `processImage(input, format)` compartido en `lib/image-processing.ts`; `processProductImage` sigue produciendo JPEG sin cambios, `processCategoryImage` produce WebP (nueva capacidad).
- [x] 6.2 Implementar subida/reemplazo/eliminación en el bucket público, reutilizando `modules/storage/service.ts` (misma clave de patrón `categories/${categoryId}/cover-${random}.${extension}`). Implementado en `modules/catalog/category-service.ts#saveCategoryImage()` (subida y reemplazo unificados — sube el objeto nuevo, persiste en Postgres, y solo entonces borra el anterior) y `#deleteCategoryImage()` (idempotente). Requirió agregar `Category.imageStorageKey` (migración aditiva/nullable) — `imagePath` por sí solo no permite borrar/reemplazar el objeto de forma segura.
- [x] 6.3 Extender `CategoryForm.tsx` con el widget de subida/previsualización/reemplazo/eliminación (reemplaza el `<input>` de texto plano actual para `imagePath`). Nuevo `components/admin/CategoryImageManager.tsx`, gated en `categoryId` existente — mismo patrón ya establecido por `CategoryAttributesManager` ("guarda la categoría primero"), no un caso nuevo a resolver.
- [x] 6.4 Confirmar el fallback público ya existente (placeholder gris cuando `imagePath` es null) sigue funcionando sin cambios. Confirmado — `app/catalogo/page.tsx` no fue modificado; el fallback ya era data-driven, sin cambio de código necesario.
- [x] 6.5 Auditoría: la subida de imagen se registra como parte de `category.updated` (sin acción nueva). Implementado — `saveCategoryImage`/`deleteCategoryImage` registran `category.updated` con `metadata.imageAction` (`uploaded`/`replaced`/`removed`), nunca binarios/URLs firmadas/rutas locales.
- [x] 6.6 Prueba: subir/reemplazar/eliminar una imagen de categoría; MIME inválido rechazado; tamaño excedido rechazado. Unit (`tests/category-image-service.test.ts`, `tests/image-processing.test.ts`, `tests/storage-service.test.ts`, `tests/category-picker-mapper.test.ts`, `tests/admin-categories-actions.test.ts`), integración con Postgres+MinIO reales (`tests-integration/category-image-storage.test.ts`), E2E admin+público (`e2e/admin/category-image.spec.ts`, `e2e/public/category-cards.spec.ts`) — todas en verde.

## 7. Contenido de cristales y tratamientos (nueva, ver design.md → "Contenido de cristales, tratamientos y opciones adicionales")

- [x] 7.1 Definir `LENS_TYPES`/`LENS_TYPE_LABELS`/`LENS_TYPE_DESCRIPTIONS` (Monofocal, Bifocal, **Progresivo** — reemplaza "Multifocal") en un módulo de código nuevo. Implementado en `modules/requests/lens-types.ts` (incluye además `LENS_TYPE_LABELS_PLURAL`, `LENS_TYPE_DETAILS`, `LENS_COMPARISON_TABLE`, `LENS_COMPARISON_SUMMARY` — fuente única server-side para `/cristales`).
- [x] 7.2 Definir `TREATMENTS` (Antirreflejo, Filtro de luz azul-violeta, Fotocromático, Protección UV, Mayor resistencia a rayaduras — nunca "completamente antirrayas"). Implementado en `modules/requests/treatments-content.ts` — módulo separado de `ADDITIONAL_OPTIONS`, nunca mezclado en el dominio. "Hidrofóbico y oleofóbico" fue retirado del catálogo por decisión comercial del propietario del producto (ver 7.9 e iteración correctiva de interfaz) — el total definitivo de tratamientos es 5, no 6.
- [x] 7.3 Definir `ADDITIONAL_OPTIONS` (Alto índice, Polarizado, Degradado, Espejado, Solares graduados con sus tres variantes: solar monofocal, solar progresivo, polarizado graduado cuando exista compatibilidad). Implementado en `modules/requests/additional-options.ts`.
- [x] 7.4 Definir `quoteOptionsSchema` (Zod, versionado) y la tabla de compatibilidad por categoría (Lentes ópticos vs. Lentes de sol) descrita en design.md. Implementado junto con la Fase 9 (motor de compatibilidades) en `modules/catalog/quote-options.ts` — ver 9.1 para el detalle. Antes de implementar se corrigieron dos contradicciones internas de design.md (sketch de `quoteOptionsSchema` vs. tabla comercial; "UV400" sin ID estable) — ver design.md → "Fase 9, motor de compatibilidades: dos contradicciones internas...".
- [x] 7.5 Reconstruir la tabla comparativa de `/cristales` con los tres tipos definitivos, accesible (texto "Sí"/"No", no solo color/✓/—). Implementado con `<caption>` (sr-only), `<th scope="col">`/`<th scope="row">` y celdas de texto literal "Sí"/"No" — sin depender de color, ✓ o —.
- [x] 7.6 Actualizar todas las ocurrencias de "Multifocal" listadas en design.md (`GLASS_TYPES`, `QuoteWizard.tsx`, `/cristales`, `/faq`, ficha de oferta) a "Progresivo" — **excepto** valores ya persistidos en `Request.details` históricos, que no se reescriben. Auditoría exhaustiva completada (`grep` recursivo): las 6 ocurrencias de código identificadas se actualizaron; ninguna fila histórica fue tocada (sin `UPDATE` sobre `Request.details`); sin mapper de presentación agregado — `RequestCard.tsx` ya renderiza el string persistido tal cual, por lo que una fila legada con `"Multifocal"` simplemente se muestra igual, sin error (verificado con prueba dedicada).
- [x] 7.7 Prueba: el enum `glassType` acepta `Progresivo` y rechaza `Multifocal` para nuevas solicitudes; una fila histórica con `Multifocal` se sigue leyendo/mostrando sin error en el panel admin. Cubierto por `tests/requests-schemas.test.ts` (acepta/rechaza) y `tests/request-card-legacy-glass-type.test.tsx` (renderizado histórico sin error) + `tests-integration/quote-requests.test.ts` (persistencia real end-to-end con "Progresivo").
- [x] 7.8 Prueba: la tabla comparativa es navegable/entendible sin depender de color (verificación de accesibilidad). Cubierto por `tests/cristales-page.test.tsx` (estructura semántica) + `e2e/public/lens-types.spec.ts` (navegación real, teclado, responsive) + axe (`/cristales` agregado a `e2e/a11y/public-pages.spec.ts`, sin hallazgos serious/critical).
- [x] 7.9 (NUEVA — iteración correctiva de interfaz, alcance exclusivamente visual, no funcional) Rediseñar la presentación de `/cristales` (hero, tarjetas de tipos de cristal, contenedor de la tabla, tratamientos, opciones para lentes de sol, CTA final) para cumplir el estándar visual de marca de Pepi Visión, sin modificar el contenido óptico ya aprobado en 7.1–7.8, y retirar "Hidrofóbico y oleofóbico" de `TREATMENTS` por decisión comercial del propietario del producto (ver design.md → "Retiro de 'Hidrofóbico y oleofóbico'"). Validación GUI manual obligatoria en desktop/tablet/mobile (no basta con que pasen las pruebas automatizadas) — completada mediante capturas reales vía Playwright mostrando hero/tarjetas/tabla/tratamientos/opciones de sol/CTA en los tres breakpoints, incluyendo la corrección de dos íconos que fallaban visualmente (Bifocal/Progresivo) pese a ser SVG sintácticamente válidos, y la corrección de un defecto real de contraste AA (`KindBadge` "Opción adicional") detectado únicamente por la corrida completa de axe. Cubierto por `tests/cristales-page.test.tsx` (15 pruebas, reescritas para la nueva estructura), `tests/treatments-and-options-content.test.ts` (5 tratamientos exactos, retiro verificado), `e2e/public/lens-types.spec.ts` (incl. "Opciones para lentes de sol" y ausencia de "Hidrofóbico y oleofóbico"), axe (serious/critical: 0), Lighthouse (sin regresión).
- [x] 7.10 (NUEVA — segunda iteración correctiva de interfaz, alcance exclusivamente visual, no funcional) El propietario del producto rechazó el resultado estético de 7.9 pese a que todos sus tests pasaban (ver CLAUDE.md → "GUI acceptance gate", regla 7: rechazo del propietario reabre la fase, no se avanza aunque los tests estén verdes). Refinar aún más `/cristales` — en particular, reemplazar la grilla de 6 tarjetas idénticas de "Tratamientos principales" por un layout de lista en dos columnas (icono + nombre + badge + descripción, con divisores en vez de tarjetas repetidas) y añadir una fila breve de propuestas de valor al hero — sin cambiar contenido óptico ni retirar/agregar tratamientos u opciones. Corrige además la referencia residual a "Hidrofóbico y oleofóbico" en `specs/lens-configuration/spec.md` (dos escenarios de la Fase 9, no tocados por 7.9). Ver design.md → "segunda iteración correctiva de interfaz de `/cristales`". **Aceptación GUI otorgada explícitamente por el propietario del producto**, tras revisión personal de `/cristales` en desktop (1440×900), tablet (768×1024) y mobile (390×844) — cubriendo responsive, tabla comparativa, tratamientos, opciones para lentes de sol y CTA. Marcada completa únicamente por esa aprobación explícita, no por el paso de los tests automatizados (ver CLAUDE.md → "GUI acceptance gate", regla 8).

## 8. Catálogo público adaptado a dos categorías

- [x] 8.1 (reabre 5.3 original) Actualizar `findDefaultPublicOfferingForProductSlug`: preferir `lentes-opticos` en vez de `armazones` como categoría por defecto para el redirect legado. Cubierto por `tests/offering-repository-legacy-redirect.test.ts` (incluye el caso de una fila stale que aún referencia `armazones`).
- [x] 8.2 (reemplaza 5.4 original) Actualizar `CATEGORY_CTA_LABELS` a exactamente `{'lentes-opticos': 'Configurar lentes', 'lentes-de-sol': 'Configurar lentes de sol'}`. Cubierto por `tests/catalog.test.ts`.
- [x] 8.3 (extiende 5.2 original) Actualizar todo el copy hardcodeado que menciona las tres categorías antiguas (metadata de `/catalogo`, `app/page.tsx`, `app/catalogo/[categorySlug]/[offeringSlug]/page.tsx`) a las dos categorías definitivas. Revisado exhaustivamente (`grep` de "tres categorías"/"armazones, lentes"): solo `/catalogo`'s metadata tenía el copy de tres categorías — corregido. `app/page.tsx` y la ficha de oferta usan "armazón" en sentido genérico (el producto físico), no como nombre de categoría — sin cambios necesarios ahí.
- [x] 8.4 (extiende 5.6 original) Actualizar fixtures E2E (`e2e/global-setup.ts`, `e2e/public/catalog.spec.ts`, `e2e/public/forms.spec.ts`, `e2e/a11y/public-pages.spec.ts`) para sembrar/afirmar contra `lentes-opticos`/`lentes-de-sol` en vez de `armazones`. `lighthouserc.cjs` también actualizado (mismo motivo).
- [x] 8.5 Prueba: una URL antigua `/catalogo/armazones/[slug]` (publicada antes del remapeo) redirige correctamente a `/catalogo/lentes-opticos/[slug]` tras la migración. E2E (`e2e/public/catalog.spec.ts`) y unit (`tests/catalog-offering-resolution.test.ts`) — ambas en verde.
- [x] 8.6 Confirmar (sin nueva prueba, por herencia de 5.1/5.5/5.7): el catálogo offering-first, "también disponible como", y la navegación responsive siguen funcionando sin cambio de código contra dos categorías. Confirmado: suite E2E completa en verde sin tocar `modules/catalog/service.ts`'s arquitectura offering-first, `OtherCategoryOfferings`, ni `CatalogFilters` responsive.
- [x] 8.7 **Gap identificado en esta replanificación**: `app/catalogo/[categorySlug]/[offeringSlug]/page.tsx` hoy no tiene ninguna lógica de fallback para un segmento de categoría legado (a diferencia de `app/catalogo/[categorySlug]/page.tsx`, que sí intenta resolver un slug de producto legado antes de devolver 404). Agregar ese mismo fallback de 3 segmentos: si `categorySlug` no resuelve a una categoría activa, intentar resolver `offeringSlug` como un slug de producto legado y redirigir 308 a su ubicación nueva antes de devolver 404. Implementado en `modules/catalog/service.ts#resolveOfferingPage()`; deliberadamente NO activa el fallback cuando `categorySlug` sí es válido (evita que un producto capture el slug de una categoría vigente) — cubierto por `tests/catalog-offering-resolution.test.ts` y el E2E de compatibilidad de URLs legadas.
- [x] 8.8 **Gap identificado en el cierre técnico posterior**: `/catalogo/armazones` y `/catalogo/lentes-de-sol-opticos` (2 segmentos, sin oferta) devolvían 404 sin ambigüedad documentada — la spec solo cubría el caso de 3 segmentos. Resuelto a favor de un redirect permanente (preferido por compatibilidad/SEO): mapa cerrado `modules/catalog/legacy-slugs.ts#resolveLegacyCategorySlug` (`armazones`→`lentes-opticos`, `lentes-de-sol-opticos`→`lentes-de-sol`), resuelto en `app/catalogo/[categorySlug]/page.tsx` **antes** del fallback de `Product.slug` — un producto no puede capturar estos dos slugs reservados. Spec `catalog-navigation` actualizada con el nuevo Requirement "Retired category slugs redirect permanently to their replacement category" (CLOSED). Cubierto por `tests/legacy-category-slugs.test.ts` (unit) y `e2e/public/catalog.spec.ts` (E2E, incluye el caso de un producto con slug coincidente).

## 9. Motor de compatibilidades

- [x] 9.1 Implementar la resolución server-side de `quoteOptions` por categoría (nunca confiar en un valor de tipo de cristal/tratamiento/opción enviado por el cliente que no esté en el allowlist de la categoría resuelta). Implementado en `modules/catalog/quote-options.ts` (motor puro, sin Prisma/React — `quoteOptionsSchema`, `resolveCategoryQuoteOptions`, `validateLensSelection`, `isLensSelectionCompatible`, `getEffectiveOfferingLensOptions`) y `modules/catalog/quote-options-service.ts` (capa server-aware — `getCategoryLensOptions`, `resolveAndValidateLensSelection` — resuelve categoryId/categorySlug/offeringId contra PostgreSQL, reutiliza el helper de pertenencia oferta↔categoría↔producto de la Fase 3). `Category.capabilities` gana el campo opcional `quoteOptions` (`modules/catalog/category-capabilities.ts`). **No integrado todavía** en `modules/requests/schemas.ts`, `QuoteWizard.tsx` ni `modules/requests/service.ts` — esa integración visual/de persistencia pertenece a la Fase 10 (cotizador configurable).
  **Cierre técnico (tres riesgos resueltos antes de consolidar, ver design.md → "cierre técnico de la Fase 9"):** (a) auditadas las dos categorías reales contra PostgreSQL — ambas carecían de `quoteOptions` (sembradas el 2026-07-15, antes de que el campo existiera; `getCategoryLensOptions()` devolvía la allowlist vacía contra la base real); (b) `modules/catalog/quote-options-reconciliation.ts` (`reconcileCanonicalQuoteOptions()`, idempotente, invocable vía `npm run catalog:reconcile-quote-options` o integrada al final de `seedCategories()` — nunca desde un request público) agrega/preserva/reporta-sin-sobrescribir según corresponda; ejecutada dos veces contra la base real (primera corrida: agregó; segunda: preservó sin cambios) — ambas categorías reales ya resuelven una matriz válida vía `getCategoryLensOptions()`; (c) `uv400` se revirtió de "alias de `proteccion-uv`" a un ID **independiente** (`SOLAR_TREATMENT_IDS` en `quote-options.ts`) — Lentes ópticos solo admite `proteccion-uv`, Lentes de sol solo admite `uv400`, sin alias implícito; se agregó `ProductOffering.configuration.lensOptionExclusions` (campo opcional, `modules/catalog/offering-configuration.ts`) para que una oferta puntual pueda excluir (nunca ampliar) tratamientos/opciones de su categoría.
- [x] 9.2 Prueba: una combinación categoría+tipo de cristal no permitida es rechazada antes de persistir o enviar correo. Cubierto por `tests/quote-options.test.ts` (unitario, motor puro), `tests/quote-options-reconciliation.test.ts` (unitario, decisión de reconciliación pura), `tests-integration/quote-options.test.ts` (categorías/productos/ofertas sintéticas) y `tests-integration/quote-options-reconciliation.test.ts` (contra las dos categorías canónicas REALES, snapshot/restore exacto — nunca las modifica destructivamente).
- [x] 9.3 Prueba: cada categoría expone exactamente las opciones de su fila en la tabla de compatibilidad (Lentes ópticos: Monofocal/Bifocal/Progresivo + tratamientos completos; Lentes de sol: sin graduación/solar monofocal/solar progresivo + UV400/polarizado/degradado/espejado/solar graduado). Cubierto por `tests/quote-options.test.ts` ("matriz comercial"); `uv400` es ahora un ID independiente de `proteccion-uv` (ver 7.4/design.md, corrección revertida).

## 10. Cotizador configurable

- [x] 10.1 Definir `STEP_DEFINITIONS` y la lógica de pasos activos filtrados por `capabilities` (sin cambios de diseño respecto al original, ver design.md → "Precios y cotizador configurable"). Implementado en `computeActiveSteps()` (`components/quote/QuoteWizard.tsx`) — único cálculo de pasos, sin ramas hardcodeadas por `categorySlug`; deriva `category`/`offering`/`color`/`lens`/`treatments`/`additionalOptions`/`prescription`/`prescriptionAttachment`/`contact`/`summary` a partir de `capabilities` + `effectiveOptions` + las respuestas ya dadas.
- [x] 10.2 Reconstruir `QuoteWizard.tsx` como un único componente configurable — reemplaza los pasos actualmente hardcodeados (`GLASS_DESCRIPTIONS`, `TREATMENTS` fijos en el componente) por lecturas del catálogo de la Fase 7 y el motor de compatibilidades de la Fase 9. Reescrito íntegramente; consume directamente `LENS_MODALITY_LABELS`/`requiresPrescription`/`isLensModalitySelectable`/`isTreatmentSelectable`/`isAdditionalOptionSelectable`/`getEffectiveOfferingLensOptions` desde `modules/catalog/quote-options.ts` (módulo puro, sin Prisma) — nunca una segunda allowlist en el cliente. Controles reales (`<input type="radio"/checkbox">` con `aria-label`, `fieldset`/`legend`), estados de carga/error explícitos para la resolución de oferta, limpieza de selecciones incompatibles con aviso accesible (`aria-live`) al cambiar de categoría/oferta/modalidad.
- [x] 10.3 Agregar el paso "opciones adicionales" (nuevo, no existía en el diseño original de tres categorías) gated por la categoría resuelta. Paso `additionalOptions` activo cuando `effectiveOptions.additionalOptions.length > 0`; diferenciado visual y semánticamente de "tratamientos" (badge "Opción adicional", fieldset/legend propios).
- [x] 10.4 Reutilizar el paso de adjunto de receta sin cambios, activo únicamente cuando `allowsPrescription && allowsPrescriptionAttachment`. Corregido durante la validación GUI real (ver más abajo): el paso además exige `hasPrescription === 'Sí'` — antes de la corrección se mostraba incondicionalmente en cuanto la capability estaba activa, incluso tras responder "No"/"No estoy seguro".
- [x] 10.5 Actualizar `modules/requests/service.ts#submitQuote` para re-resolver categoría/oferta/producto/marca/color server-side, autorizar campos según `capabilities` + `quoteOptions` reales de la categoría resuelta; el precio del snapshot se resuelve aquí desde `ProductOffering.priceFromClp`. `submitQuote` reescrito: resuelve `Category` vía `findCategoryById` (nunca confía en `categorySlug`/`categoryId` del cliente sin re-resolución), resuelve `ProductOffering` vía `getQuoteOfferingContext` (fail-closed: activo/visible/pertenencia a la categoría/color perteneciente al producto), valida `lensModality`/`treatments`/`additionalOptions` contra `effectiveOptions` con las primitivas de la Fase 9, resuelve id→label solo en el servidor antes de persistir (`Request.details` mantiene los campos V1 sin cambio semántico, gana claves aditivas `categoryId`/`categoryName`/`categorySlug`/`offeringId`/`priceFromSnapshot`/`additionalOptions`/`additionalOptionLabels`).
- [x] 10.6 Prueba: los dos flujos (Lentes ópticos / Lentes de sol) muestran exactamente los pasos y opciones esperados. `tests/quote-wizard.test.tsx` (12 pruebas de componente): Monofocal/Bifocal/Progresivo nunca Multifocal; Sin graduación/Solar monofocal/Solar progresivo nunca Bifocal; tratamientos ópticos nunca muestran Polarizado ni Hidrofóbico; Alto índice se muestra como opción adicional, no como tratamiento. `tests-integration/quote-requests.test.ts` 10.6 (flujo completo con oferta real).
- [x] 10.7 Prueba: un par categoría/oferta manipulado es rechazado; un color que no pertenece al producto resuelto es rechazado. `tests-integration/quote-requests.test.ts` 10.7 (oferta/categoría no correspondiente rechazada; color de otro producto rechazado).
- [x] 10.8 Prueba: un campo condicionado a una capability o a una opción no otorgada por la categoría nunca se persiste ni se envía por correo. `tests-integration/quote-requests.test.ts` 10.8 (tratamiento fuera del allowlist de la oferta/categoría rechazado antes de `createRequest`/`sendAndLog`).
- [x] 10.9 Prueba: la ruta de "asesoría" omite los pasos dependientes de un producto específico. `tests-integration/quote-requests.test.ts` 10.9 (ninguna oferta/color se persiste en la ruta de asesoría); `tests/quote-wizard.test.tsx` (paso "color" ausente cuando `frameChoice !== 'catalog'`).
- [x] 10.10 **NUEVA — aceptación GUI del cotizador** (ver CLAUDE.md → "GUI acceptance gate"): validación manual obligatoria de `/cotizador` en desktop (1440×900), tablet (768×1024) y mobile (390×844) para los flujos óptico, solar sin graduación y solar graduado, más zoom 200% y el estado de error sin categoría. Durante la revisión previa a esta aceptación se detectaron y corrigieron dos defectos funcionales reales encontrados únicamente al ejecutar el flujo en un navegador real (ver 10.4 y la nota de `stepIndex`/`useEffect` de precarga en `QuoteWizard.tsx`).
  **Iteración correctiva de UX (posterior a la primera revisión GUI) — ambos hallazgos corregidos antes de la aprobación final**: (a) el nav de progreso recortaba el último rótulo de paso ("Resumen" → "R") en mobile (390px) a partir de 8 pasos — reemplazado, exclusivamente en ese breakpoint (`sm:hidden`), por un indicador compacto ("Paso X de Y" + nombre completo del paso actual + barra `role="progressbar"` con `aria-valuemin/max/now/valuetext`), sin tocar el stepper completo de desktop/tablet (`hidden sm:flex`) — ambos leen la misma `steps` de `computeActiveSteps()`, nunca una segunda lista; (b) seleccionar una categoría avanzaba automáticamente de paso, inconsistente con el resto de los pasos de selección única — corregido: `selectCategory()` ya no llama `setStepIndex`, y el botón "Continuar" del paso de categoría queda `disabled` hasta que haya una categoría seleccionada (ver design.md → "Fase 10, iteración correctiva de UX"). Cobertura agregada: 6 pruebas de componente nuevas (`tests/quote-wizard.test.tsx`) y un nuevo spec E2E dedicado a mobile (`e2e/public/quote-wizard-mobile.spec.ts`, viewport 390×844, incluye un caso de teclado) — 512 unit / 70 integración / 95 E2E+a11y (94 pasan + 1 skip preexistente no relacionado) en verde; Lighthouse ad hoc contra `/cotizador` (no está en la lista fija de CI) pasa los mismos umbrales del proyecto.
  **Aceptación GUI otorgada explícitamente por el propietario del producto**, tras revisión personal de `/cotizador` en desktop (1440×900), tablet (768×1024) y mobile (390×844), cubriendo: el indicador responsive de pasos (stepper completo en desktop/tablet, indicador compacto en mobile); el flujo de Lentes ópticos; el flujo solar sin graduación; el flujo solar graduado; la entrada directa a `/cotizador`; la entrada desde una `ProductOffering`; el paso de resumen; zoom 200%; navegación por teclado; y la ausencia de scroll horizontal. Marcada completa únicamente por esa aprobación explícita, no por el paso de la suite automatizada (ver CLAUDE.md → "GUI acceptance gate", regla 8).

## 11. Snapshot de solicitudes

- [ ] 11.1 Extender `Request.details` con el snapshot de categoría/oferta descrito en `request-category-snapshot`, incluyendo `priceFromSnapshot` resuelto desde `ProductOffering.priceFromClp` — inmutable después.
- [ ] 11.2 Agregar filtro por categoría al listado administrativo de solicitudes.
- [ ] 11.3 Prueba: el snapshot histórico no cambia si luego se edita la categoría/oferta/precio.
- [ ] 11.4 Prueba: el filtro por categoría del inbox administrativo reduce correctamente los resultados.

## 12. Filtros dinámicos (sin dependencia de taxonomía — sin cambios respecto al diseño original)

- [ ] 12.1 Constructor de schema de filtros dinámico por categoría a partir de `CategoryAttributeDefinition`.
- [ ] 12.2 Escopar los filtros comunes existentes dentro de la categoría seleccionada.
- [ ] 12.3 Resolución de query params por allowlist.
- [ ] 12.4 Actualizar `CatalogFilters.tsx` (escritorio + drawer móvil).
- [ ] 12.5 Prueba: query param desconocido descartado; atributo no filtrable no filtrable.
- [ ] 12.6 Prueba: filtros comunes + dinámicos combinados; valor malformado descartado sin error de servidor.

## 13. Emails y WhatsApp — únicamente en cuanto consumen el snapshot de categoría

- [ ] 13.1 Actualizar `quote-customer-confirmation.ts` / `quote-business-notification.ts` (HTML + texto plano) con los campos de categoría/oferta del snapshot de la Fase 11, incluyendo el precio ya snapshotteado.
- [ ] 13.2 Actualizar el mensaje de WhatsApp con el contexto de categoría, mismo precio snapshotteado.
- [ ] 13.3 Prueba: los correos (HTML y texto) incluyen la categoría (Lentes ópticos / Lentes de sol); la mención de receta adjunta no expone archivo, storageKey ni URL privada.

Nota: el rediseño más profundo de plantillas (separación cliente/negocio más allá de lo ya implementado, horario comercial, entrega del logo) vive en el cambio independiente `improve-transactional-emails` — ver design.md → "Dependencias con otros cambios OpenSpec".

## 14. SEO y compatibilidad de rutas

- [ ] 14.1 Metadata por categoría y por oferta, evitando contenido duplicado entre las dos categorías del mismo producto.
- [ ] 14.2 Canonical, `BreadcrumbList`, `ItemList`, `Product`/`Offer` JSON-LD — precio exclusivo de `ProductOffering.priceFromClp`.
- [ ] 14.3 Sitemap (dos categorías + ofertas activas/visibles, sin entradas duplicadas para rutas legadas que solo redirigen — incluyendo las URLs `/catalogo/armazones/*` ya publicadas antes del remapeo).
- [ ] 14.4 Prueba: el mismo producto en Lentes ópticos y Lentes de sol produce dos títulos/canonical distintos.
- [ ] 14.5 Prueba: una oferta sin precio público omite el precio en el schema `Offer`.

## 15. Migración, backfill y corte definitivo

- [ ] 15.1 Ejecutar el script de remapeo de taxonomía (Fase 5) contra una copia de los datos actuales; verificar idempotencia corriendo dos veces.
- [ ] 15.2 Regresión manual completa: cada URL de producto/oferta previamente publicada (incluyendo `/catalogo/armazones/*`) sigue resolviendo.
- [ ] 15.3 Suite completa: lint, typecheck, tests, build — todo en verde dentro de Docker Compose.
- [ ] 15.4 Validación manual en Mailpit de las plantillas de correo actualizadas (categoría y "Progresivo" visibles en HTML y texto).
- [ ] 15.5 `openspec validate redesign-extensible-catalog-v2 --strict`.
- [ ] 15.6 `git diff --check`; confirmar que `design-reference/` permanece sin cambios.
- [ ] 15.7 Confirmar que ningún código de lectura/escritura pública referencia `Product.priceFromClp` fuera del seed histórico.

## 16. Pruebas y cierre

- [ ] 16.1 Regresión completa de accesibilidad (axe) sobre `/catalogo`, `/cristales`, `/cotizador` con el contenido nuevo.
- [ ] 16.2 Regresión completa de Lighthouse sobre las páginas afectadas.
- [ ] 16.3 Confirmar que ninguna referencia a "Multifocal" queda fuera de datos históricos inmutables (ver Fase 7.6).
- [ ] 16.4 Informe final para revisión y aprobación — no archivar el cambio hasta que la implementación sea revisada y aprobada explícitamente.

## Tabla de trazabilidad

| Tarea anterior | Nuevo identificador | Estado | Razón del cambio |
|---|---|---|---|
| 0.1 | 0.1 | sin cambio | — |
| 1.1–1.5 | 1.1–1.5 | sin cambio | — |
| 2.1 | 2.1 | sin cambio | Agnóstica al conteo de categorías |
| 2.2 | 5.4 | **reabierta** | El seed de tres categorías ya no es correcto; reemplazado por el seed de dos categorías |
| 2.3–2.4 | 2.3–2.4 | sin cambio | Comportamiento de idempotencia/fail-closed, agnóstico |
| 3.1–3.9 | 3.1–3.9 | sin cambio | Arquitectura `ProductOffering`, agnóstica a la taxonomía |
| 4.1–4.6 | 4.1–4.6 | sin cambio | Administración de categorías/ofertas, agnóstica al conteo |
| — | 4.7 | **nueva** | Imagen de categoría en el formulario admin (no existía) |
| 5.1 | 8.6 (heredada) | sigue válida | Arquitectura offering-first, sin cambio de código |
| 5.2 | 8.3 | **parcialmente reabierta** | Rutas válidas; copy de tres categorías debe actualizarse |
| 5.3 | 8.1 | **reabierta** | Default de redirect legado hardcodeado a `armazones` |
| 5.4 | 8.2 | **reemplazada** | Mapa de CTA hardcodeado a tres categorías |
| 5.5 | 8.6 (heredada) | sigue válida | Cross-links entre categorías, agnóstico al conteo |
| 5.6 | 8.4, 8.5 | **parcialmente reabierta** | Escenario válido; fixtures/asserts deben actualizarse |
| 5.7 | 8.6 (heredada) | sigue válida | Responsive/estado vacío, agnóstico al conteo |
| 6.1–6.6 | 12.1–12.6 | sin cambio (pendiente) | Filtros dinámicos, sin dependencia de taxonomía |
| 7.1–7.8 | 10.1, 10.2, 10.4–10.9 | reescrita | Se agrega el motor de compatibilidades (Fase 9) y el paso de opciones adicionales (10.3) |
| 8.1 | 11.1 | reescrita | Mismo snapshot, ahora explícitamente ligado a la taxonomía de dos categorías |
| 8.2–8.3 | 13.1–13.2 | reescrita | Separadas del resto del snapshot; alcance acotado a "solo consumir el snapshot" (rediseño más profundo vive en `improve-transactional-emails`) |
| 8.4 | 11.2 | sin cambio | — |
| 8.5–8.7 | 11.3–11.4, 13.3 | sin cambio | — |
| 9.1–9.5 | 14.1–14.5 | reescrita | Slugs de categoría actualizados; incluye las URLs legadas de `armazones` |
| 10.1–10.8 | 15.1–15.7, 16.1–16.4 | reescrita | El backfill original (aditivo) se reemplaza por el remapeo (Fase 5); se separa el cierre final en su propio bloque |
| — | 5.1–5.7 | **nuevas** | Corrección de taxonomía a dos categorías — no existía en el plan original |
| — | 6.1–6.6 | **nuevas** | Imágenes de categoría — no existía en el plan original |
| — | 7.1–7.8 | **nuevas** | Contenido de cristales/tratamientos/opciones — no existía en el plan original |
| — | 9.1–9.3 | **nuevas** | Motor de compatibilidades — no existía en el plan original |

**Total de tareas**: 66 antes (`grep -c '^\- \[[ x]\]'` sobre el `tasks.md` previo a este cambio) → **94 después** (mismo conteo sobre este archivo). **Progreso real recalculado**: **24 tareas completadas** (`[x]`: bloque 0 completo, bloque 1 completo, bloque 2 parcial —2.1/2.3/2.4—, bloque 3 completo, bloque 4 completo) de **94 totales**, equivalente a **25.5%** — comparado con el 32/66 (48.5%) que reportaba el `tasks.md` anterior. La caída de porcentaje refleja fielmente que: (a) la Fase 5 original (7 tareas, todas `[x]`) se reabre/redistribuye en el bloque 8 (6 tareas, todas `[ ]` hasta verificarse contra la nueva taxonomía), (b) la tarea 2.2 se reabre como 5.4, y (c) se agregan 28 tareas netas nuevas (bloques 5, 6, 7, 9 y la extensión 4.7) que no existían en el plan de tres categorías.

**Actualización posterior (segunda iteración correctiva de interfaz de `/cristales`, 7.10)**: el conteo de este párrafo quedó desactualizado por adiciones previas (7.9 ya lo había dejado en 97 sin recalcular esta nota). Conteo real en ese momento (`grep -c '^\- \[[ x]\]' tasks.md`): **98 tareas totales**, **54/98 completadas** (`[x]`) — 7.10 se marcó completa tras aceptación GUI explícita del propietario del producto, que revisó personalmente `/cristales` en desktop (1440×900), tablet (768×1024) y mobile (390×844), incluyendo responsive, tabla comparativa, tratamientos, opciones para lentes de sol y CTA.

**Actualización posterior (Fase 9, motor de compatibilidades — 7.4, 9.1, 9.2, 9.3)**: conteo real actual (`grep -c '^\- \[[ x]\]' tasks.md`): **98 tareas totales**, **58/98 completadas** (`[x]`). Alcance exclusivamente de dominio/servidor — no funcional visible; sin cambios de GUI en `/cristales` ni en el cotizador (gate visual de CLAUDE.md no aplica a esta pasada, ver design.md). No se avanzó al cotizador condicional (Fase 10) ni a snapshots de solicitudes (Fase 11).

**Actualización posterior (cierre técnico de la Fase 9 — auditoría, reconciliación, separación de `uv400`)**: mismo conteo, **98 tareas totales**, **58/98 completadas** — ninguna tarea nueva se agregó ni se marcó; este cierre resolvió tres riesgos dentro del alcance ya cubierto por 7.4/9.1–9.3 (ver design.md → "cierre técnico de la Fase 9" y las notas actualizadas de 9.1–9.3 arriba). Las dos categorías canónicas reales (`lentes-opticos`, `lentes-de-sol`) ahora resuelven una matriz `quoteOptions` válida vía `getCategoryLensOptions()`, confirmado contra PostgreSQL real.

**Actualización posterior (Fase 10, adaptación condicional del cotizador — 10.1–10.9)**: conteo real actual (`grep -c '^\- \[[ x]\]' tasks.md`): **99 tareas totales** (se agregó 10.10, aceptación GUI, que no existía antes), **67/99 completadas** (`[x]`). `QuoteWizard.tsx`, `modules/requests/schemas.ts`, `modules/requests/service.ts` y `app/cotizador/{actions,page}.tsx` fueron reescritos para consumir exclusivamente el motor de compatibilidades de la Fase 9 (sin segunda allowlist en la GUI); toda entrada al cotizador (directa, desde ficha de oferta, desde `/cristales`) re-resuelve categoría/oferta/producto/color server-side, nunca confía en query params ni en estado del cliente. Suite completa en verde dentro de Docker Compose: lint, typecheck, 506 pruebas unitarias, 70 de integración, build, y las 91 pruebas E2E/a11y (comando exacto de CI) — incluyendo dos correcciones reales encontradas únicamente al ejecutar el flujo en un navegador real (ver 10.4). **10.10 (aceptación GUI) permanece pendiente**: no se marca hasta la aprobación explícita del propietario del producto, conforme a CLAUDE.md → "GUI acceptance gate". No se avanzó a la Fase 11 (snapshot histórico), filtros dinámicos, emails, WhatsApp, cotizaciones PDF, marcas, SEO final, backfill ni al corte definitivo de `Product.priceFromClp`; el cambio no fue archivado; no se hizo commit.

**Actualización posterior (iteración correctiva de UX sobre la Fase 10, previa a solicitar la aprobación GUI final)**: mismo conteo, **99 tareas totales**, **67/99 completadas** — ninguna tarea nueva se agregó ni se marcó; 10.10 sigue `[ ]`. Se corrigieron los dos hallazgos de UX de la revisión GUI anterior (indicador de progreso mobile recortado; avance automático inconsistente al elegir categoría) — ver la nota actualizada de 10.10 arriba y design.md → "Fase 10, iteración correctiva de UX". Suite completa re-verificada en verde dentro de Docker Compose (lint, typecheck, 512 unit, 70 integración, build, 95 E2E/a11y, axe, Lighthouse ad hoc de `/cotizador`, `npm audit`, `openspec validate` del cambio y de `--specs`, `git diff --check`). Sin cambios en `openspec/specs/`, `openspec/changes/archive/` ni `design-reference/`; sin commit.

**Actualización posterior (cierre documental de la Fase 10 — aceptación GUI otorgada)**: conteo real actual (`grep -c '^\- \[[ x]\]' tasks.md`): **99 tareas totales**, **68/99 completadas** (`[x]`) — 10.10 se marcó completa tras aceptación GUI explícita del propietario del producto, que revisó personalmente `/cotizador` en desktop (1440×900), tablet (768×1024) y mobile (390×844), cubriendo el indicador responsive de pasos, los flujos óptico/solar sin graduación/solar graduado, la entrada directa y desde `ProductOffering`, el resumen, zoom 200%, navegación por teclado y ausencia de scroll horizontal. Sin cambios de código en este cierre. No se avanzó a la Fase 11 ni se archivó el cambio.
