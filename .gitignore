# macOS
.DS_Store

# Variables y secretos
.env
.env.*
!.env.example

# Node / Next.js
node_modules/
.next/
out/
dist/
coverage/
next-env.d.ts
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Backups locales
backups/
tmp/

# Playwright / Lighthouse CI artifacts (reports, traces, videos, screenshots,
# and the one-off test-admin credentials file written by e2e/global-setup.ts)
playwright-report/
test-results/
blob-report/
e2e/.auth/
.lighthouseci/
lhci_reports/

# Graphify — keep the shareable/queryable artifacts (graph.json, graph.html,
# GRAPH_REPORT.md, manifest.json, .graphify_labels.json,
# .graphify_labels.json.sig) versioned; ignore everything ephemeral: the
# extraction cache, dated pre-recluster backups, per-run intermediate/state
# files, and the two files that record this machine's local absolute paths.
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
graphify-out/.graphify_analysis.json
graphify-out/.graphify_ast.json
graphify-out/.graphify_chunk_*.json
graphify-out/.graphify_detect.json
graphify-out/.graphify_extract.json
graphify-out/.graphify_incremental.json
graphify-out/.graphify_old.json
graphify-out/.graphify_semantic_new.json
graphify-out/.graphify_uncached.txt
graphify-out/.graphify_python
graphify-out/.graphify_root
