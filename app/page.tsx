// Technical placeholder only — the real public site pages are implemented
// in Fase 3 (Sitio público), not here.
export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'var(--font-sans)' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', fontWeight: 700 }}>
        Pepi Visión 360 — Fundación técnica
      </h1>
      <p>
        Fase 2: Next.js, TypeScript estricto, Tailwind CSS y Prisma configurados. El
        sitio público real se construye en la Fase 3.
      </p>
    </main>
  );
}
